<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- App Sandbox (inherited by all child processes) -->
	<key>com.apple.security.app-sandbox</key>
	<true/>

	<!-- Inherit parent's ability to read/write user-selected files -->
	<key>com.apple.security.files.user-selected.read-write</key>
	<true/>

	<!-- Inherit network client access -->
	<key>com.apple.security.network.client</key>
	<true/>

	<!-- Electron-specific entitlements for Helper processes -->
	<!-- Allow JIT compilation for V8 JavaScript engine -->
	<key>com.apple.security.cs.allow-jit</key>
	<true/>

	<!-- Allow unsigned executable memory for V8 JavaScript engine -->
	<key>com.apple.security.cs.allow-unsigned-executable-memory</key>
	<true/>

	<!-- Disable library validation (required for Electron framework) -->
	<key>com.apple.security.cs.disable-library-validation</key>
	<true/>
</dict>
</plist>
